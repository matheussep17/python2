{
  "name": "igreja-license-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "deploy": "wrangler deploy --config wrangler.toml",
    "db:init:local": "wrangler d1 execute igreja-licenses --local --config wrangler.toml --file=schema.sql",
    "db:init:remote": "wrangler d1 execute igreja-licenses --remote --config wrangler.toml --file=schema.sql"
  },
  "dependencies": {
    "scrypt-js": "3.0.1"
  },
  "devDependencies": {
    "wrangler": "4.100.0"
  },
  "overrides": {
    "esbuild": "0.28.1"
  }
}
